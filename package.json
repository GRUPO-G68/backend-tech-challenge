{
  "name": "sistema",
  "version": "1.0.0",
  "description": "sistema para fiap",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -L .",
    "start": "node .",
    "migrate": "node migration.js up --migrate-all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mariadb": "^3.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
